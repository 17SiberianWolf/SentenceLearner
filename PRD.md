# SentenceLearner 项目需求文档（PRD）

> 版本：v1.0
> 状态：调研完成 / 需求冻结（MVP 范围已定）
> 日期：2026-09-17
> 维护人：项目 Owner（用户本人）

---

## 0. 参考项目调研（Why this project）

本项目并非从零造轮子，而是借鉴成熟开源打字练习产品的思路，把训练单位从"单词"升级为"完整句子"。

| 项目 | 地址 | 与本项目关系 | 可借鉴点 |
|------|------|--------------|----------|
| **Qwerty Learner** | github.com/RealKai42/qwerty-learner | 灵感源头 | 词级打字引擎、强制纠错、深色模式、VSCode 插件；React+TS+Vite+Tailwind，**可直接 fork 扩展** |
| **TypeWords / 词文记** | github.com/zyronon/TypeWords | 高 | 文章跟打 / 逐句输入 / 边听边默写 / 双语对照；FSRS 间隔重复；IndexedDB + Supabase 云同步 |
| **EatWords** | github.com/c-hri-sw-u/EatWords（TypeWords fork） | **极高** | 在 TypeWords 上新增 **AI 例句练习 + 造句练习**（用户用词造句，AI 1–10 评分 + 改进建议，DeepSeek/Qwen） |
| **FSI-Type-Drill** | github.com/rtyrtyrtyqw/FSI-Type-Drill | **极高** | 受 QL 启发，本地 Ollama 跑**替换 / 形态 / 转换三类句型训练**，弱点仪表盘（时态 / 语序 / 介词 / 冠词），闭环 AI 补强——最贴合"句子构建肌肉记忆" |
| **kadalingo / 咔哒英语** | github.com/JianWang97/kadalingo | 中 | Electron+React，场景短句 + 中英互译 + AI 生成短句 |
| **LiTy (Listen & Type)** | github.com/voidism/LiTy | 中 | YouTube 字幕听打 + GPT-4o 讲解（听→写闭环） |
| **Monkeytype / Typa** | monkeytype.com | 低（基础设施） | 打字测试引擎、实时速度 / 正确率、引号 / 文章模式 |

**调研结论**：
- Qwerty Learner 解决的是"单词拼写 + 键盘肌肉记忆"；本项目的痛点是**完整句子的口头 / 书面产出**，本质是句型、语法、搭配、语感——是不同的学习目标。
- 市面上最接近成品的是 **EatWords（造句 + 例句）** 和 **FSI-Type-Drill（句型训练）**，已验证"打字练句子"路线可行。
- 工程上最稳妥的路径是 **fork Qwerty Learner**，复用其成熟的打字引擎、主题与快捷键，新增"句子练习"模块。

---

## 1. 项目背景

用户日常需用英文与德国老板（汇报）及欧洲同事（邮件 / 会议）沟通，已具备 CET-6 词汇量，但**口语与听力偏弱，写不出 / 说不流畅完整英文句子**。

现有工具（如 Qwerty Learner）多停留在"背单词"层面，缺少针对**完整句子**的产出训练。用户希望沿用 Qwerty Learner「手敲练肌肉记忆」的思路，但训练对象从单词变为**完整英文句子**——通过"看中文 → 写英文 → 听发音 → 纠错 → 复习"的循环，把常用工作句子的拼写、语序和语感练成直觉。

---

## 2. 用户画像

| 维度 | 描述 |
|------|------|
| 身份 | 技术型工程师 + 项目经理 |
| 语言水平 | 已具备 CET-6 词汇；口语、听力偏弱，正针对性补强 |
| 工作场景 | 向德国老板英文汇报、与欧洲同事英文邮件 / 会议协作、工控技术英语（过程控制、机器视觉术语）、商务英语 |
| 设备习惯 | 主要在 PC 端练习，习惯键盘输入；编码间隙随手练 |
| 诉求 | 不是背单词，而是能**口头 + 书面产出完整句子** |

---

## 3. 产品定位

- **形态**：开源 Web 应用，免费、免登录、本地优先。
- **核心理念**：把 Qwerty Learner 的"手敲 + 强制纠错 + 肌肉记忆"迁移到完整英文句子的训练上。
- **差异化**：以**中文意思为输入触发**，以**听写（TTS 朗读）为辅助**，目标是句子产出而非单词拼写。
- **可扩展性**：后期可扩展为 VSCode 插件（编码间隙随手练），契合"键盘工作者"定位。

---

## 4. 核心交互闭环

```
┌─────────────────────────────────────────────────────────┐
│  1. 系统显示句子【中文意思】                               │
└───────────────────────────┬─────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│  2. 用户【手写完整英文句子】                               │
│     · 输入时实时字符 / 单词级 绿(正确)/红(错误) 反馈        │
│     · 可设置"错词 / 错句强制重打"                          │
└───────────────────────────┬─────────────────────────────┘
                            │ 不会写
                            ▼
┌─────────────────────────────────────────────────────────┐
│  3. 点击【小喇叭】→ TTS 朗读完整英文句                     │
│     · 用户根据发音补写                                     │
│     · 可反复听（与"提示"按钮二选一或并存）                 │
└───────────────────────────┬─────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│  4. 完成 → 实时校对                                        │
│     · 显示正确率、WPM、耗时、错误词                         │
└───────────────────────────┬─────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│  5. 收藏 / 加入错句本 → 下一题                             │
└─────────────────────────────────────────────────────────┘
```

**双路径说明**：
- 路径 A（直接写）：看到中文 → 直接敲出英文（练书面产出）。
- 路径 B（听写辅助）：看到中文不会写 → 点小喇叭听发音 → 根据发音写出英文（同时练听力 + 口语语感 + 拼写）。

---

## 5. 核心功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| **中译英打字** | 显示中文意思，用户输入完整英文句 | P0 |
| **朗读听写（小喇叭）** | 点击朗读完整英文句；可反复听 | P0 |
| **实时纠错** | 字符 / 单词级绿红反馈；可设"错词 / 错句强制重打" | P0 |
| **提示** | 空格键 / 提示按钮显示下一个词 / 字母（填空式辅助） | P1 |
| **难度分级** | 按英文词数自动标 初级（≤15）/ 高级（>15） | P0 |
| **收藏 / 错句本** | 错误句自动进错句本；用户可收藏重点句 | P1 |
| **统计** | 已答对 / 已答错、实时正确率、WPM、耗时 | P0 |
| **导入句子库** | 支持上传 `template.xlsx` 格式文件 | P0 |
| **分类筛选** | 按工作场景分类（会议 / 邮件 / 汇报 / 技术…）练习 | P1 |
| **TTS 发音设置** | 美 / 英音、语速、发音人 | P1 |
| **深色模式 / 指法** | 护眼 + 手型纠正 | P2 |

> **P0 = MVP 必做；P1 = 第二期；P2 = 体验增强**

---

## 6. 练习流程（详细步骤）

1. 用户选择难度（初级 / 高级）或具体分类（如"会议沟通"）。
2. 系统显示当前句子的**中文翻译**。
3. 用户在输入区敲出完整英文句。
4. 输入时实时绿 / 红反馈；敲错可设置是否强制重打（参考 QL 强制纠错机制）。
5. 若不会写，点击**小喇叭**，TTS 朗读完整英文句（Web Speech API，无需联网）。
6. 完成后显示：正确率、耗时、WPM、错误词。
7. 用户可选择**收藏**或进入**下一题**。

---

## 7. 数据规范

### 7.1 源数据（沿用用户现有 Excel）

文件路径：`resource/template.xlsx`，Sheet1。

| 列名 | 含义 | 系统用途 |
|------|------|----------|
| 原文标题 | 句子分类 / 场景 | 作为 `category` |
| 原文正文 | 标准英文句 | 作为 `en`（标准答案） |
| 译文标题 | （预留，一般同原文标题） | 备用 |
| 译文正文 | 中文意思 | 作为 `zh`（练习提示） |
| 音频地址 | 预录音频 URL（多为空） | 为空则用 TTS 生成 |

**当前数据规模（已分析）**：162 条有效记录，覆盖职场通用、会议、邮件、技术澄清、进度 / 风险 / 现场调试等 20+ 类场景。

### 7.2 系统内部扩展字段

| 字段 | 来源 | 说明 |
|------|------|------|
| `wordCount` | 内部计算 | 英文词数（按空格分词，标点不计） |
| `level` | 内部计算 | ≤15 初级；>15 高级 |
| `errorCount` | 练习累计 | 该句累计错误次数（用于错句本排序） |
| `lastPracticedAt` | 练习写入 | 上次练习时间（用于间隔重复） |
| `collected` | 用户操作 | 是否收藏 |

### 7.3 难度分级规则（关键）

- 英文词数 **≤ 15** → **初级**
- 英文词数 **> 15** → **高级**
- 分词方式：以空格切分，统计 `\b\w+\b` 数量，标点不计入。
- **当前数据实测**：162 句全部 ≤14 词（均值约 9 词），**目前 100% 属于初级阶段**；高级阶段需另行准备或组合更长 / 复合句。

---

## 8. 页面结构

| 页面 | 内容 |
|------|------|
| **练习页（核心）** | 中文提示区、英文输入区、小喇叭按钮、提示按钮、实时统计条（正确率 / WPM / 耗时）、收藏 / 下一题 |
| **句子库页** | 分类浏览、按初级 / 高级筛选、导入 / 导出 `template.xlsx` |
| **错句本页** | 记录错误句子、可重练、支持间隔重复（FSRS 可选） |
| **统计页** | 正确率趋势、WPM、高频错误词列表 |
| **设置页** | TTS 语速 / 发音人 / 美英音、键盘布局、难度偏好、强制纠错开关 |

---

## 9. 非功能需求

| 项 | 要求 |
|------|------|
| 离线可用 | 句子库可本地导入，数据存 IndexedDB；默认不上传用户数据 |
| 响应性能 | 按键反馈 < 100ms |
| 兼容性 | Chrome / Edge / Firefox 现代浏览器 |
| 隐私 | 默认不上传用户练习数据 |
| 可维护性 | fork 自 Qwerty Learner，沿用其 React+TS+Vite+Tailwind 技术栈 |

---

## 10. MVP 范围（第一轮交付）

**只做练习页闭环，不追求功能齐全。**

1. 支持导入 `resource/template.xlsx`（解析 5 列 → 内部句子对象）。
2. 按词数自动标初级（当前导入句全部为初级，高级入口预留）。
3. 中译英打字 + 小喇叭朗读（Web Speech API）+ 实时字符级绿红反馈。
4. 正确 / 错误统计 + 下一题。

**第二期（MVP 之后）**：提示填空、错句本 + 间隔重复、分类筛选、统计页、TTS 设置、收藏。
**第三期（可选）**：造句模式（用户自写句 + AI 评分，参考 EatWords）、替换 / 形态 / 转换句型训练（参考 FSI-Type-Drill）、VSCode 插件、Supabase 云同步。

---

## 11. 技术路径建议

- **推荐 fork Qwerty Learner**：复用其打字引擎、主题、快捷键与 VSCode 插件框架，新增"句子练习模式"，工作量最小、风险最低。
- **备选**：fork TypeWords（已有文章跟打底座）+ 接 EatWords 的造句逻辑。
- **TTS**：优先浏览器原生 `speechSynthesis`（免费、离线、无需密钥）；如需更好音质再集成 edge-tts / CosyVoice。
- **存储**：IndexedDB 本地存储，后续可选 Supabase 云同步。
- **AI（远期）**：造句评分可用本地 Ollama（隐私优先）或 API（DeepSeek / Qwen / Gemini）。

---

## 附录 A：微信小视频两款参考 App 截图分析

| 截图 | 核心交互 | 对本项目的启发 |
|------|----------|----------------|
| 图 1（整句填空式） | 中文句 + 英文整句按词填空，蓝色高亮当前词 / 答案 | 长句可拆成"按词显示"，降低一眼看全句的记忆压力 |
| 图 2（部分字母填空式） | 中文句 + 英文只显示部分字母（如 `i'm so h___`），底部含 朗读 / 提示 / 提交 / 收藏 / 下一题 + 正确率统计 | 确认核心元素：小喇叭朗读、提示、实时正确率、收藏 / 导航 |

**共同方向**：中文意思 → 手写英文句 → 不会就听 → 听完再写 → 统计对错。**与本项目第 4 节交互闭环一致。**

## 附录 B：数据实测结论（来自 template.xlsx）

- 有效句子：162 条。
- 字段：原文标题 / 原文正文 / 译文标题 / 译文正文 / 音频地址。
- 场景分类：职场通用、会议、邮件、技术澄清、进度汇报、风险 / 变更 / 现场调试等 20+ 类。
- 句长：全部 ≤14 英文词，均值约 9 词 → **当前全部归初级**，高级阶段需补料。