# SentenceLearner 架构决策记录 (ADR) v1.0

> 本文件记录项目关键技术决策, 含背景/决策/后果, 防止后续重议。

## ADR-001 fork Qwerty Learner vs 从零开发
- **背景**: 需要"手敲练英文"的成熟打字引擎 (字符比对 / 强制纠错 / WPM / 错词本)。QL 已具备。
- **决策**: fork QL (GPL-3.0), 新增"句子练习"模块, 复用其打字引擎。
- **后果**: + 复用率 ~70–80%、风险低、MVP 约 3–4 人日; - 须遵守 GPL-3.0, 受 QL 架构约束。

## ADR-002 GPL-3.0 许可义务
- **背景**: QL 以 GPL-3.0 发布。
- **决策**: 衍生作品以 GPL-3.0 发布, 保留协议与版权, 修改公开。
- **后果**: 本项目源码须开源; 不可在不改协议前提下闭源商业化。

## ADR-003 整句 TTS: Web Speech API vs edge-tts
- **背景**: 需"不会就听"朗读整句的能力。
- **决策**: MVP 用浏览器原生 `speechSynthesis` (离线 / 免费 / 无密钥)。
- **后果**: + 零依赖零成本; - 音色因浏览器而异、语音列表异步加载。后续如需更自然可接 edge-tts (需网络/密钥)。

## ADR-004 存储: IndexedDB 本地 vs Supabase 云同步
- **背景**: 是否跨设备同步进度。
- **决策**: MVP 仅 IndexedDB (dexie) 本地; 云同步列为后续 (可选 Supabase)。
- **后果**: + 离线 / 隐私 / 简单; - 不跨设备。可与 QL 既有 Supabase 方案后期对齐。

## ADR-005 句子建模为"超长 word"
- **背景**: QL 打字引擎按单个 `word` 字符串做字符级比对。
- **决策**: 句子 = 一个 `name`=整句的 `Word`, 默认开 `hideAll` (隐英文、显中文), 复用比对/纠错/统计。
- **后果**: + 几乎零改引擎; - 须验证 `Word.tsx` 对长 `name` 无拆词硬假设 (已列中风险, 冒烟测试消解)。