# 后续开发计划（Roadmap）

> 当前状态：MVP 全功能闭环已完成（练习/错句本/统计/复习/句型/句子库/本地存储/自动朗读/撒花反馈）。
> 下方为规划，按优先级排列；具体取舍随实际推进调整。

## 一、近期（高优先级）

### 1. 第三期：本地 Ollama · AI 辅助造句学习 ⭐
> 重心：**用 Ollama 当"教练"而非"裁判"**。AI 评分降级为可选子功能（锦上添花）。
> 详细规划见 `OLLAMA_INTEGRATION.md`（v2 修订版）。

调研结论（FSI-Type-Drill、/fsi 等）：最有效的"辅助造句"范式是 **FSI drills（同一句型反复替换/转换操练）+ 闭环补强（出错即生成针对性练习）+ 薄弱点诊断**。

功能优先级（P0 必做 / P1 强化 / P2 可选）：
- **P0 辅助造句**：① 渐进提示梯（卡壳时分 3 级提示，不直接给答案）② 句型拆解讲解（显示答案时解释结构/公式/口诀）③ FSI Drills（句型页升级为替换/转换无限变体操练）
- **P1 强化**：④ 闭环补强（写错自动生成同类变体）⑤ 薄弱点诊断看板（时态/语序/介词/冠词/词汇 5 类）⑥ 导师问答（追问"为什么"）
- **P2 可选**：⑦ 自适应练习句生成 ⑧ AI 评分（分数+改写+逐错+评语，主动触发）

- 集成：本机 Ollama（`serve.js` 零依赖代理 → `11434`），数据不出本机；默认 `qwen2.5:7b`（drill 可用 `qwen2.5:3b`）；未装模型时回退精确比对。
- 注意：需本机算力跑模型；先写集成代码 + 回退逻辑，模型由用户自选安装。

### 2. 跨设备云同步
- 用 Supabase（life-vocab-app 已有配置经验）同步 `sl_errors` / `sl_sr` / `sl_pat_*` / `sl_collected` 等。
- 与本地 `store.json` 做冲突合并（时间戳/版本号）。

### 3. 移动端 PWA
- 离线缓存 + 可安装到手机主屏，利用碎片时间练习；复用本地存储与既有关卡数据。

## 二、中期（增强）

- **VSCode 插件形态**：把练习面板嵌到编辑器侧边，利用编码间隙刻意练习。
- **发音评分**：Web Audio 录制麦克风发音，与 TTS 音素对比给出口语反馈（需 Web Speech 之外的前端音频处理）。
- **更多句型方向**：面试、会议发言、客户沟通、技术分享等场景句型库。
- **生词本**：从错句/收藏自动抽取生词，联动 life-vocab-app 词库做记忆。

## 三、打磨与工程化

- **导出练习记录**：Excel / CSV 导出（复用既有零依赖 xlsx 写入器）。
- **暗色模式**：跟随系统主题。
- **单元测试 + 轻量 CI**：把 `smoke.test.js` / `verify_*.js` 纳入 CI。
- **句子内容运营**：继续扩充生活/商务/工控语料，按用户实际薄弱点定向加句。
- **国际化**：界面文案英文版（面向想练中文的英文用户？暂低优先）。

## 四、已知限制（上线前须知）

- 撒花/动画/自动朗读需在真实浏览器验证（沙箱无法实跑）。
- 首句自动朗读受浏览器 TTS「需用户手势」策略限制，首次交互后各题正常发声。
- 仅英文 TTS；中文输入/判分不在范围。
- 许可证待补充（参考 Qwerty Learner 思路自研，需明确 GPL/MIT 等并加 LICENSE）。

## 五、可选探索

- 句子 → 词级拆解统计（哪些词总写错，定向强化）。
- 社区句子库（用户共享自制句，需后端/审核，优先级低）。